version: '3.8'

services:
  # Векторна база даних (наш "мозок")
  qdrant:
    image: qdrant/qdrant:latest
    container_name: coremind-qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage
    restart: always